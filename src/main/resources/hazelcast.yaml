hazelcast:
  cluster-name: hazelcast-cluster
  network:
    join:
      tcp-ip:
        enabled: true
        member-list:
          - 0.hazelcast-embedded.apps.internal
          - 1.hazelcast-embedded.apps.internal
          - 2.hazelcast-embedded.apps.internal
